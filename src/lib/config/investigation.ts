/**
 * US-2.7: Investigation Configuration
 *
 * Shared constants for investigation voting system.
 * Used by both server-side logic and front-end display.
 */

/** Credits reserved/charged per investigation vote */
export const INVESTIGATION_COST = 50;

/** Threshold for investigation to trigger (fraction of destinations) */
export const INVESTIGATION_THRESHOLD = 2 / 3;
